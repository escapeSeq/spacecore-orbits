# Spacecore Orbits — narration script

Voice: en-GB-LibbyNeural · rate +5% · live interactive capture + animated title cards

## 01-open
Illustration: hero

Hi — welcome to Spacecore Orbits. This live three-D Earth simulator puts your controls on the left and the globe on the right, so orbits, beams, and coverage stay in view as the scene moves.

## 02-map
Illustration: map
Themes: “Time & speed”, “Satellites & orbits”, “Coverage tools”, “Theme & exports”

We'll walk the live app through four themes. First, time and speed — the clock, pause, and how you accelerate the scene so motion is easy to follow. Next, satellites and orbits — adding a sample bird, pasting your own TLE data, and watching paths unfold on the globe. Then coverage tools — the global layers, beams, and the elevation mask that shapes the footprint. And finally, theme and exports — switching light or dark, then taking a model, an SVG, or the TLE text with you.

## 03-app
Clip: `03-app.mp4`
Marked UI text: “Control panel”, “3D Earth”

Here's the workspace: controls on the left, the live globe on the right. Drag to orbit the camera, scroll to zoom, and right-drag to pan when you want a closer look.

## 04-time
Clip: `04-time.mp4`
Marked UI text: “Date/time”, “Instructions”

Under the title, the simulation clock stays with the scene. Beside it, Instructions opens this walkthrough, and you can share a link that brings someone straight back here.

## 05-add-tle
Clip: `05-add-tle.mp4`
Marked UI text: “Popular”, “sample”

Under Popular samples, one click places a real elliptical satellite on the globe — path, coverage, and beam appear together so you can see the orbit take shape.

## 06-speed
Clip: `06-speed.mp4`
Marked UI text: “Simulation Speed”, “Pause”

Now we push simulation speed right up — watch that long orbit race ahead as the multiplier climbs. Pause freezes the frame when you want a closer look; Resume lets the motion continue.

## 07-theme
Clip: `07-theme.mp4`
Marked UI text: “Theme toggle”

Top-right is the theme toggle: a light high-contrast sky when you need clarity, then back to dark space when you want that classic look.

## 08-satellite
Clip: `08-satellite.mp4`
Marked UI text: “Orbit”, “Coverage”, “Beam”

Swing the camera and the eccentric path keeps moving — footprint and beam sweeping across the Earth so the geometry is easy to read from another angle.

## 09-global
Clip: `09-global.mp4`
Marked UI text: “Orbits”, “Coverage”, “Beams”, “Globe”, “Grid”

Global Controls let you blink coverage, beams, orbits, and the grid off and on — handy when you want a quieter view or need every layer back at once.

## 10-elevation
Clip: `10-elevation.mp4`
Marked UI text: “Minimum Elevation Angle”

The elevation slider models a ground antenna mask. Raise it and the usable footprint shrinks; bring it back down for maximum coverage again.

## 12-paste
Clip: `12-paste.mp4`
Marked UI text: “Add TLE Satellite(s)”

When you're ready for your own data, Add TLE Satellite opens a paste box for bulk NORAD lines — optional name, then line one and line two — as many blocks as you need.

## 13-exports
Clip: `13-exports.mp4`
Marked UI text: “Exports”, “3D Model”, “2D SVG”, “TLE”

Exports gathers the ways out: a three-D model, a two-D SVG snapshot, an SVG animation, or the raw TLE text for satellites you're showing.

## 14-loop
Illustration: loop

So the rhythm is simple: add a satellite, raise speed until the orbit tells its story, tune coverage and elevation, then export or share what you've built.
